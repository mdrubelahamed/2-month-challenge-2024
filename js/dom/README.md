Web element manipulation

window => dom, bom, js

# BOM

    screen
    navigator
    location

### // window object (dig deep in it)

window.location
window.outerWidth

### // Difference : Arrow function & normaal function

=> in arrow function when we use `console.log(this)` it points towards the global window object  
but,  
in normal function when we use `console.log(this)` it points towards the context who calling this object
